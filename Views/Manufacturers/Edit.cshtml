@model Manufacturer

@{
    ViewData["Title"] = "Update manufacturer";
}

<div class="row text">
    <div class="col-md-8 offset-2">
        <p>
            <h1>Update manufacturer</h1>
        </p>
        <div class="row">
            <div class="col-md-8 offset-2">
                <form asp-action="Edit">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                    <div class="form-group text-center">
                        <img id="ManufacturerLogoURLPreview" class="border-info rounded-circle" style="max-width: 150px" />
                    </div>
                    <div class="form-group">
                        <label asp-for="ManufacturerLogoURL" class="control-label"></label>
                        <input asp-for="ManufacturerLogoURL" class="form-control" />
                        <span asp-validation-for="ManufacturerLogoURL" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="Name" class="control-label"></label>
                        <input asp-for="Name" class="form-control" />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="FoundationYear" class="control-label"></label>
                        <input asp-for="FoundationYear" class="form-control" />
                        <span asp-validation-for="FoundationYear" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <input type="submit" value="Update" class="btn btn-outline-success float-right" />
                        <a class="btn btn-outline-secondary" asp-action="Index">Show All</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts{ 
    <script>

        $(document).ready(function () {
            var output = document.getElementById('ManufacturerLogoURLPreview');
            output.src = $("#ManufacturerLogoURL").val();
        })

        $("#ManufacturerLogoURL").on("change", function () {
            var output = document.getElementById('ManufacturerLogoURLPreview');
            output.src = $(this).val();
        })
    </script>
}